$('.delete_item').bind('ajax:success', function() {
   $(this).closest('tr').fadeOut();
   //$('#cart_sub_total').value(<%= @sub_total%>);
});

$('#cart').html(' ('+<%= current_user.cart_items.count%>+')')


// debugger
// $("#flash").html();